name: Sync FAQ from Confluence

on:
  schedule:
    - cron: '0 6 * * *'  # Daily at 2pm SGT (UTC+8)
  workflow_dispatch:       # Manual trigger

env:
  TARGET_FILE: "overview/faq.md"
  PR_BASE_BRANCH: "main"
  CONFLUENCE_PAGE_ID: ${{ secrets.CONFLUENCE_PAGE_ID }}
  CONFLUENCE_BASE_URL: ${{ secrets.CONFLUENCE_BASE_URL }}
  CONFLUENCE_USERNAME: ${{ secrets.CONFLUENCE_USERNAME }}
  CONFLUENCE_API_TOKEN: ${{ secrets.CONFLUENCE_API_TOKEN }}

jobs:
  sync-faq:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Fetch FAQ from Confluence and convert to Markdown
        run: |
          pip install html2text --quiet

          curl -s \
            -u "${CONFLUENCE_USERNAME}:${CONFLUENCE_API_TOKEN}" \
            "${CONFLUENCE_BASE_URL}/wiki/rest/api/content/${CONFLUENCE_PAGE_ID}?expand=body.export_view" \
            -o confluence_response.json

          # Extract HTML and convert to Markdown
          python3 << EOF
          import json, html2text, os

          with open("confluence_response.json") as f:
              data = json.load(f)

          html = data["body"]["export_view"]["value"]

          h = html2text.HTML2Text()
          h.ignore_links = False
          markdown = h.handle(html)

          with open(os.environ["TARGET_FILE"], "w") as f:
              f.write(markdown)

          print("Done — faq.md written")
          EOF

          rm confluence_response.json

      - name: Create PR if changes
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "docs: sync FAQ from Confluence"
          title: "docs: sync FAQ from Confluence"
          body: "Automated sync of FAQ content from Confluence page ${{ env.CONFLUENCE_PAGE_ID }}."
          branch: confluence-faq-sync
          base: ${{ env.PR_BASE_BRANCH }}
