name: Runner canary
on:
  push:
    branches: [PLAY-1089-confluence-faq-sync]
  workflow_dispatch:
jobs:
  ping:
    runs-on: [self-hosted, da-team]
    timeout-minutes: 3
    steps:
      - run: echo "runner alive on $(hostname)"
